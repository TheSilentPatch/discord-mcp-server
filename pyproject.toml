[project]
name = "discord-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.15.0",
    "py-cord>=2.6.1",
    "python-dotenv>=1.1.1",
]
